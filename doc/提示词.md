sandbox_mode = "workspace-write"
approval_policy = "on-request"
会话权限问题


1.我想做个类似hermes客户端的项目, 先从最简单的开始, 只有对话的客户端, 有临时记忆, 
    需要设置模型用线上的kimi code plan就行, api请求地址是https://api.kimi.com/coding/v1

2.在这个项目的基础上, 我想设计左侧的菜单功能, 分为两个区块, 上区块有新对话,  插件等(后续会扩展,预留可扩展功能), 下区块是会话历史,右面聊天框的输入消息的文本框上面加一个选择文件夹功能, 类似于选择或指定当前对话所关联的项目文件夹, 每次对话加一个思考的描述, (chat思考中...x秒 x随秒表递增), 右上的设置做成侧边导航布局,, 暂时先就有一个功能, 菜单名:模型,接口密钥,API 请求地址,选用模型,检测联通等不变, 点击设置的关闭才退出, 点其他地方不退出

3.优化
    1.会话历史可以对每个名称进行修改
    2.主窗口最上面关联项目文件夹左边的标题可以去掉

4.实现统一工具调用框架的功能

